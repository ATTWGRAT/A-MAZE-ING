SRCDIR = solver

BINDIR = bin

SRC = $(wildcard$(SRCDIR)/*.c)

CC = gcc

CFLAGS = -Wall -g

TARGET = $(BINDIR)/run_solver

all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $@ $^

$(shell mkdir -p $(BINDIR))

clean:
	rm -rf $(BINDIR)/run_solver
