SRCDIR = preprocessor

BINDIR = bin

SRC = $(wildcard$(SRCDIR)/*.c)

CC = gcc

CFLAGS = -Wall -g

TARGET = $(BINDIR)/run_preprocessor

all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $@ $^

$(shell mkdir -p $(BINDIR))

clean:
	rm -rf $(BINDIR)/run_preprocessor
